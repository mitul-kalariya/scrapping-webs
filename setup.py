from setuptools import setup, find_packages

setup(
    name='crwbastillepost',
    author='Newton',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'scrapy',
    ],
)
