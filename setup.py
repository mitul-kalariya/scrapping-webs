from setuptools import setup, find_packages

setup(
    name='crwmbnnewsonline',
    author='Newton',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'scrapy',
        'webdriver_manager',
        'webdriver_manager',
    ],
)
