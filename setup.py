from setuptools import setup, find_packages

setup(
    name='crw20minutesonline',
    author='Newton',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'scrapy',
    ],
)
